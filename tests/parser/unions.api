
type name = union { a: u32  };
type name = union { a: u32, };

type name = union { a: u32, b: u32  };
type name = union { a: u32, b: u32, };

type name = union { a: u32, b: u32, c: u32  };
type name = union { a: u32, b: u32, c: u32, };
